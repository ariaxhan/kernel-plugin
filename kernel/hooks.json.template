{
  "PreCompact": [
    {
      "matcher": "*",
      "hooks": [
        {
          "type": "prompt",
          "prompt": "Before compaction, extract conversation facts in ARBITER SYNTAX per .claude/rules/arbiter-syntax.md. Extract: 1) Project setup (use_<tech>), 2) Architecture (api_is_<type>), 3) Conventions (require_<practice>), 4) Business logic (<condition> -> <result>), 5) Decisions (decided_<choice>). Output ONLY arbiter syntax statements, no prose. Then compress via arbiter.py at .claude/tools/arbiter.py"
        }
      ]
    }
  ],
  "PostToolUse": [
    {
      "matcher": "Write|Edit",
      "hooks": []
    }
  ],
  "PreToolUse": [
    {
      "matcher": "Bash",
      "hooks": []
    }
  ],
  "Stop": [
    {
      "matcher": "*",
      "hooks": []
    }
  ],
  "SessionStart": [
    {
      "matcher": "*",
      "hooks": []
    }
  ]
}
